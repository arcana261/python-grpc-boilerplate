#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if [ "$USE_SYSTEM_PYTHON" = "1" ]; then
	/usr/bin/env sh -c "pipenv $@"
else
	$DIR/../build/python/bin/exec "pipenv $@"
fi
